# Вход в Swagger и получение списка заявок

## 🎯 Цель

Войти под пользователем и получить список заявок через Swagger UI

---

## Шаг 1: Откройте Swagger UI

**URL**: `http://localhost:3000/api-docs`

---

## Шаг 2: Найдите POST /api/auth/sign_in

1. Прокрутите до секции **Auth**
2. Найдите **POST /api/auth/sign_in**
3. Нажмите на него, чтобы развернуть

---

## Шаг 3: Войдите как пользователь

1. Нажмите **"Try it out"** (кнопка справа)
2. В поле **Request body** введите:

   ```json
   D jndВ
   ```

3. Нажмите синюю кнопку **"Execute"**

---

## Шаг 4: Скопируйте токен

В разделе **Responses** вы увидите:

```json
{
  "token": "eyJhbGciOiJIUzI1NiJ9.eyJ1c2VyX2lkIjo2LCJlbWFpb...",
  "user": {
    "id": 6,
    "email": "demo@local",
    "moderator": false
  }
}
```

**СКОПИРУЙТЕ** весь токен (длинная строка после `"token":`)

---

## Шаг 5: Authorize в Swagger

1. Прокрутите в самый верх страницы
2. Найдите кнопку **🔓 Authorize** (справа от логотипа)
3. Нажмите на неё
4. В поле **Value** вставьте токен (БЕЗ кавычек, БЕЗ слова "Bearer")
5. Нажмите **"Authorize"**
6. Нажмите **"Close"**

## Замок должен быть закрыт

🔒 Теперь замок должен быть закрыт

---

## Шаг 6: Получите список заявок

1. Прокрутите до секции **BeamDeflections**
2. Найдите **GET /api/beam_deflections**
3. Нажмите на него, чтобы развернуть
4. Нажмите **"Try it out"**
5. (Опционально) Установите параметры:
   - `status`: formed
   - `page`: 1
   - `per_page`: 20
6. Нажмите **"Execute"**

---

## ✅ Ожидаемый результат

```json
{
  "beam_deflections": [
    {
      "id": 22,
      "status": "formed",
      "formed_at": "2025-11-29T...",
      "completed_at": null,
      "length_m": 5.0,
      "udl_kn_m": 10.0,
      "note": "Тестовая заявка для API",
      "creator_login": "demo@local",
      "moderator_login": null,
      "result_deflection_mm": null
    }
  ],
  "meta": {
    "current_page": 1,
    "next_page": null,
    "prev_page": null,
    "total_pages": 1,
    "total_count": 1
  }
}
```

**HTTP Status**: 200 OK

---

## 🔍 Что вы увидите

- **1 заявку** (ID: 22) в статусе `formed`
- Создатель: `demo@local` (это ваш пользователь)
- Обычные пользователи видят **только свои заявки**

---

## 📊 Дополнительные действия

### Детали заявки

`GET /api/beam_deflections/22`

### Фильтрация по статусу

`GET /api/beam_deflections?status=formed`

### Обновление заявки

`PUT /api/beam_deflections/22`

```json
{
  "beam_deflection": {
    "note": "Обновлённое примечание"
  }
}
```

### Удаление заявки

`DELETE /api/beam_deflections/22`

---

## ❓ Проблемы?

## Ошибка 401 Unauthorized

- Проверьте, что вы сделали **Authorize** с токеном
- Попробуйте заново войти и получить новый токен

## Пустой список заявок

- Проверьте, что тестовая заявка создана (ID: 22)
- Попробуйте ### Детали заявки

`GET /api/beam_deflections/22` - должна вернуть 200

## Проблемы с токеном

- Убедитесь, что скопировали токен полностью
- Не добавляйте "Bearer " - только сам токен
- Попробуйте выйти (sign_out) и войти заново

---

## 🎉 Готово

Теперь вы можете:

- ✅ Войти в систему через API
- ✅ Получить список своих заявок
- ✅ Просматривать детали
- ✅ Обновлять и удалять заявки
